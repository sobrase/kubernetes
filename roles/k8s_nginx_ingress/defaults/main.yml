---
# Directory containing nginx ingress images saved as tar archives
# Use the common offline directory so all components share the same base path
nginx_ingress_image_dir: "{{ offline_dir }}/nginx_ingress/images"
